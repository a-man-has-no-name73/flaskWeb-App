# Primary requirements (try this first)
Flask==2.3.3
numpy==1.24.4
tensorflow==2.13.0
Werkzeug==2.3.7
gunicorn==21.2.0

# Alternative requirements (if above fails)
# Flask==3.0.0
# numpy==1.26.0
# tensorflow==2.15.0
# Werkzeug==3.0.0
# gunicorn==21.2.0

# Minimal fallback (if TensorFlow fails)
# Flask
# numpy
# tensorflow-cpu
# Werkzeug
# gunicorn
